import { useTestStore as useTestStoreBase } from '../stores';

/**
 * @hook useTestStore
 * @description Hook to access the test store
 * @returns The test store hook
 */
export const useTestStore = useTestStoreBase;
