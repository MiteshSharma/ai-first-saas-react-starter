/**
 * @function testUtilFunction
 * @description A test utility function to verify path aliases
 * @param {string} input - The input string to process
 * @returns {string} The processed string
 */
export const testUtilFunction = (input: string): string =>
  `Processed: ${input}`;

/**
 * @function calculateSum
 * @description A simple function for testing purposes
 * @param {number} a - First number
 * @param {number} b - Second number
 * @returns {number} The sum of a and b
 */
export const calculateSum = (a: number, b: number): number => a + b;
